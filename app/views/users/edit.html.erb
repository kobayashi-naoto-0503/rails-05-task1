<div class="topic-new-wrapper">
  <div class="container">
    <div class="row">
      <div class="col-md-6 col-md-offset-3">
        <h1 class="text-center">User Edit</h1>
        <%= form_for @user  do |f| %>
        <div class="form-group">
          <%= f.label :image %>
          <%= f.file_field :image %>
        </div>
        <div class="form-group">
          <%= f.label :hobby %>
          <%= f.text_area:hobby, class: 'form-control' %>
        </div>
        <% if logged_in? %>
          <%= f.submit '更新', class: 'btn btn-black btn-block' %> <%# submitは自動でupdateかcreateかを判断してくれる。formメソッドのおかげ %>
          <%= link_to "削除する", "/user/#{@user.id}", method: :delete %>
          <%# 削除する際のroutingはpathが指定されていなかったら、urlの指定（"/topics/#{@topic.id}"）で書かないとdestroyアクションを呼べない %>
          <%= link_to "戻る", user_path %>
        <% end %>
        
        <% end %>
      </div>
    </div>
  </div>
</div>